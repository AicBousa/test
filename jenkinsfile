pipeline {
   agent any

   stages {
      stage('Checkout: Code') {
         steps {
            echo 'Hello World'
         }
      }
   }
   
    post {
        always {
             sh """ date >> artifact.txt """
           archiveArtifacts 'artifact.txt'
        }
    }
   
}
